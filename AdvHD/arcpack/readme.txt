pack .arc files (advhd)

tested on miazora interstellar focus (rio1.arc), it worked

arcpack.py	can take many in-parameters, call with "python arcpack.py"
arcpack2.py	takes one in-parameter, call with "py arcpack2.py"

if you have trouble with wildcard parameters expanding (or not), choose the
version that matches your expanding behaviour
