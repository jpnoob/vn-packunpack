pack .pac files (softpal adv system engine)

the plan is to support both formats (the old one used by flyable heart, and
the new one used by pretty much every other game)

destination file must be the old version of the archive, since we need the
magical bytes at the start of the file
